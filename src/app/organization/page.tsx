import BaseWrapper from "@/components/BaseWrapper";
import FeedPage from "./components/page";

export default function Organization(){

    return (
        <BaseWrapper>
            <FeedPage/>
        </BaseWrapper>
    )}
